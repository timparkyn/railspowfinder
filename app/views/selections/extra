

<!--
@stations.each do |station|
  <div class='checkbox'>
  </div>
    <% end %>
  </div>

user_selections_path
f??? check_box_tag user[station_ids][], station.id, f.object.stations.include?(station)
station.description

f.collection_check_boxes(:station_ids, Station.all, :id, :description) do |station|
station.label { station.check_box }



button_to("Update",
  user_selections_path(id: station.id),
  {class: 'btn btn-primary btn-sm btn-block'})
  -->
