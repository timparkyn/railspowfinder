<div class="btn-group-vertical ">
  <% @unselected_stations.each do |unselected_station| %>
    <div>
        <%= button_to(unselected_station.description,
          user_selections_path(id: unselected_station.id),
          {class: 'btn btn-primary btn-block'}) %>
    </div>
  <% end %>
</div>
