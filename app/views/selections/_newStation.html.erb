


<%= form_for(@selections) do |f| %>

    <%= f.collection_check_boxes(:station_ids, Selection.all, :code, :description) do |stn| %>
      <%= stn.label { stn.check_box } %>
    <% end %>

      <%= f.submit ("Add station")%>
<% end %>
